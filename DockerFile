# 1. Imagem base com Node
FROM node:18-alpine

# 2. Diretório de trabalho dentro do container
WORKDIR /app

# 3. Copiar package.json e instalar dependências
COPY package*.json ./
RUN npm install

# 4. Copiar o resto do código
COPY . .

# 5. Expôr a porta do Vite
EXPOSE 5173

# 6. Correr em modo desenvolvimento
CMD ["npm", "run", "dev"]
